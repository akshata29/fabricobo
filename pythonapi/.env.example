# ════════════════════════════════════════════════════════════════
# FabricObo Python API — Environment Variables
#
# Copy this file to .env and fill in your values.
# All configuration is loaded from this file (no appsettings.json needed).
#
# These settings are equivalent to appsettings.json in the .NET API.
# ════════════════════════════════════════════════════════════════

# ── Entra ID (Azure AD) Configuration ──
AZUREAD_INSTANCE=https://login.microsoftonline.com/
AZUREAD_TENANT_ID=
AZUREAD_CLIENT_ID=
AZUREAD_CLIENT_SECRET=
AZUREAD_AUDIENCE=
AZUREAD_SCOPES=access_as_user

# ── Azure AI Foundry Agent Configuration (v2 Responses API) ──
FOUNDRY_PROJECT_ENDPOINT=
FOUNDRY_MODEL_DEPLOYMENT_NAME=chat4o
FOUNDRY_FABRIC_CONNECTION_ID=
FOUNDRY_AGENT_NAME=FabricOboAgent
FOUNDRY_API_VERSION=2025-05-15-preview
FOUNDRY_RESPONSE_TIMEOUT_SECONDS=180

# ── SPA Authentication (public values, served via /api/config) ──
SPA_TENANT_ID=
SPA_SPA_CLIENT_ID=
SPA_API_CLIENT_ID=

# Test users shown in SPA login panel (JSON array)
SPA_TEST_USERS_JSON=[{"label":"Test User A","upn":"usera@yourtenant.onmicrosoft.com","description":"REP001 — test user with RLS filter"},{"label":"Test User B","upn":"userb@yourtenant.onmicrosoft.com","description":"REP002 — test user with RLS filter"}]

# ── Entitlement users — stub user-to-RepCode mappings (JSON array) ──
ENTITLEMENT_USERS_JSON=[{"upn":"usera@yourtenant.onmicrosoft.com","rep_code":"REP001","role":"Advisor"},{"upn":"userb@yourtenant.onmicrosoft.com","rep_code":"REP002","role":"Advisor"}]

# ── Bot Framework (Teams / Copilot Studio) ──
BOT_MICROSOFT_APP_ID=
BOT_MICROSOFT_APP_PASSWORD=
BOT_MICROSOFT_APP_TENANT_ID=
BOT_MICROSOFT_APP_TYPE=SingleTenant
BOT_OAUTH_CONNECTION_NAME=

# ── CORS (comma-separated origins) ──
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
